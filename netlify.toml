[build]
	publish = "dist/"
	command = "npm run build"
	[build.environment]
		NODE_VERSION = "20"

	[build.processing]
		skip_processing = false

[[edge_functions]]
	path = "/"
	function = "easteregg"
